services:
  - type: web
    name: investnath-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn Backend.wsgi:application
    envVars:
      - key: DJANGO_SECRET_KEY
        value: django-insecure-@5myg-kz(@b#rucnd4$^c@j$um7b0$kbn)34z$e#)*mta%#e2z

      - key: DJANGO_DEBUG
        value: False
      - key: DATABASE_URL
        fromDatabase:
          name: investnath-db
          property: connectionString
